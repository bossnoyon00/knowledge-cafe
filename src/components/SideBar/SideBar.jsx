import React from 'react';

const SideBar = (props) => {
    
    return (
        <div>
            <h2 className='text-center'>Spend time on read :{number}  </h2>
        </div>
    );
};

export default SideBar;